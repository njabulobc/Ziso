name: ziso_mobile
version: 1.0.0+1
description: Offline-first recreation of the Ziso web platform for mobile devices.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.1
  intl: ^0.18.1
  pdf: ^3.10.7
  excel: ^4.0.2
  share_plus: ^7.2.1
  flutter_secure_storage: ^9.0.0
  collection: ^1.18.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/schema.sql
